# REST-APIs
REST is an architectural style that defines a set of constraints to be used for creating web services.
RESTful APIs are used to perform the CRUD (create, read, update, delete) operations.
